enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, delivered, pending, cancelled }

enum PaymentMethod {
  bKash,
  paypal,
  googlePay,
  applePay,
  visa,
  masterCard,
  creditCard,
  payStack,
}
